{
  hardware.graphics = {
    enable = true;
    enable32Bit = true;
  };
  
  # chaotic.mesa-git = {
  #   # TODO: Move to Gaming.
  #   enable = true;
  #   #fallbackSpecialisation = false;

  #   extraPackages = with pkgs; [ mesa_git.opencl ];
  # };
}
